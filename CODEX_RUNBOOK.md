# CODEX RUNBOOK — Гайд для AI-агентов

Этот документ описывает пошаговый процесс разработки English-Liminal MVP для AI-агентов (Codex, Claude, Cursor, etc).

---

## Общая информация

**Проект:** English-Liminal
**Цель:** 30-дневный MVP мобильного приложения для изучения английского через контекстные микро-сценарии
**Стек:** React Native (Expo) + TypeScript, AsyncStorage, expo-speech/av, YAML
**Управление задачами:** См. [ISSUES.md](./ISSUES.md)

---

## Быстрый старт для агента

### 1. Клонирование и установка

```bash
# Репозиторий уже клонирован
cd /path/to/English-Liminal-

# Установка зависимостей (после создания package.json)
pnpm install

# Запуск Expo Dev Server
pnpm expo start
```

### 2. Структура задач

Все задачи описаны в [ISSUES.md](./ISSUES.md) и разбиты на Milestones:
- **Milestone 0**: Инициализация проекта и каркас
- **Milestone 1**: Scripts Layer (YAML → движок)
- **Milestone 2**: Голос и оценка произношения
- **Milestone 3**: Утро/Пинг/Вечер UX-потоки
- **Milestone 4**: Память-волна (затухание и пинги)
- **Milestone 5**: Данные и аналитика
- **Milestone 6**: Контент (5+ YAML-сцен)

### 3. Приоритеты

**Сначала:** Milestone 0-3 (Issues 1-9) — это P0 для MVP
**Потом:** Milestone 4-5 (Issues 10-13) — P1
**Опционально:** Milestone 6 (Issues 14-15) — P2

---

## Пошаговая инструкция

### Шаг 1: Issue 1 — Инициализация React Native (Expo)

**Цель:** Создать базовый Expo проект с TypeScript и установить зависимости.

**Действия:**
1. Перейти в корень репозитория
2. Создать Expo проект:
   ```bash
   cd app
   npx create-expo-app . --template blank-typescript
   ```
3. Установить зависимости:
   ```bash
   pnpm add expo-speech expo-av expo-notifications @react-native-async-storage/async-storage js-yaml zod
   pnpm add -D @types/js-yaml eslint prettier husky lint-staged
   ```
4. Настроить ESLint/Prettier:
   - Создать `.eslintrc.js`
   - Создать `.prettierrc`
5. Настроить Husky для pre-commit:
   ```bash
   pnpm add -D husky
   npx husky install
   npx husky add .husky/pre-commit "pnpm lint-staged"
   ```
6. Создать `lint-staged` конфиг в `package.json`

**Проверка:**
```bash
pnpm expo start
# Должен запуститься без ошибок
```

**Коммит:**
```bash
git add .
git commit -m "feat: initialize Expo project with TypeScript and dependencies (Issue #1)"
```

---

### Шаг 2: Issue 2 — Структура экранов и навигация

**Цель:** Создать навигацию и заглушки экранов.

**Действия:**
1. Установить React Navigation:
   ```bash
   pnpm add @react-navigation/native @react-navigation/bottom-tabs
   pnpm add react-native-screens react-native-safe-area-context
   ```
2. Создать экраны в `app/src/screens/`:
   - `HomeScreen.tsx`
   - `WarmupScreen.tsx`
   - `PingScreen.tsx`
   - `RinseScreen.tsx`
   - `SettingsScreen.tsx`
3. Создать навигацию `app/src/navigation/AppNavigator.tsx`:
   - Bottom Tabs с 5 экранами
4. Обновить `App.tsx` для использования навигации

**Проверка:**
```bash
pnpm expo start
# Проверить переключение между табами
```

**Коммит:**
```bash
git add .
git commit -m "feat: add navigation and screen structure (Issue #2)"
```

---

### Шаг 3: Issue 3 — Парсер YAML-сценариев

**Цель:** Реализовать загрузку и валидацию YAML-сценариев.

**Действия:**
1. Создать `app/src/lib/scripts.ts`:
   - Определить TypeScript типы: `Step`, `Script`
   - Функция `loadScript(id: string): Script`
   - Валидация через zod
2. Создать тестовый YAML: `app/src/data/morning-warmup-01.yaml`
3. Реализовать кэширование в памяти
4. Функция `listAvailableScripts()`

**Проверка:**
```typescript
// В любом экране
import { loadScript } from '../lib/scripts';
const script = loadScript('morning-warmup-01');
console.log(script);
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement YAML script parser and validator (Issue #3)"
```

---

### Шаг 4: Issue 4 — Исполнитель шагов (step runner)

**Цель:** Хук для управления шагами сценария.

**Действия:**
1. Создать `app/src/hooks/useScriptRunner.ts`
2. Реализовать логику:
   - `currentStep`, `stepIndex`, `progress`
   - `next()`, `prev()`, `reset()`
   - `isComplete`
3. Использовать `useState` для состояния

**Проверка:**
```typescript
// В WarmupScreen
const { currentStep, next, progress } = useScriptRunner('morning-warmup-01');
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement useScriptRunner hook for step management (Issue #4)"
```

---

### Шаг 5: Issue 5 — TTS (прослушай и повтори)

**Цель:** Компонент для воспроизведения текста через TTS.

**Действия:**
1. Создать `app/src/components/TTSButton.tsx`
2. Интеграция с `expo-speech`:
   ```typescript
   import * as Speech from 'expo-speech';
   Speech.speak(text);
   ```
3. UI: play/pause, визуальный индикатор
4. Состояния: `idle`, `playing`, `paused`

**Проверка:**
```typescript
// Использовать в любом экране
<TTSButton text="I'll be there in an hour" />
```

**Коммит:**
```bash
git add .
git commit -m "feat: add TTS component with play/pause controls (Issue #5)"
```

---

### Шаг 6: Issue 6 — Запись и простая оценка речи

**Цель:** Компонент для записи голоса и оценки произношения.

**Действия:**
1. Создать `app/src/components/Recorder.tsx`
2. Интеграция с `expo-av` для записи
3. MVP-оценка: Levenshtein distance
4. Функция `calculateScore(userText, refText)` в `app/src/lib/scoring.ts`
5. Возврат score (0-100) и подсказки

**Проверка:**
```typescript
// В WarmupScreen
<Recorder refText="I'll be there in an hour" onScore={(score) => console.log(score)} />
```

**Коммит:**
```bash
git add .
git commit -m "feat: add voice recorder with pronunciation scoring (Issue #6)"
```

---

### Шаг 7: Issue 7 — WarmupScreen (3 минуты)

**Цель:** Полный UX-поток утреннего разогрева.

**Действия:**
1. Обновить `WarmupScreen.tsx`:
   - Использовать `useScriptRunner`
   - Рендер компонентов для каждого типа шага
2. Создать компоненты шагов:
   - `ListenStep.tsx` (TTSButton)
   - `SpeakCheckStep.tsx` (Recorder)
   - `ContrastStep.tsx` (выбор варианта)
   - `ApplyToLifeStep.tsx` (голос/текст)
3. Добавить таймер и прогресс-бар

**Проверка:**
```bash
# Пройти полный сценарий вручную
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement WarmupScreen with full scenario flow (Issue #7)"
```

---

### Шаг 8: Issue 8 — PingScreen (60 секунд контекст-пинг)

**Цель:** Быстрый микро-сценарий.

**Действия:**
1. Обновить `PingScreen.tsx`:
   - Карточка с фразой
   - Быстрый цикл: TTS → Recorder → оценка
   - Таймер 60 сек
2. Добавить кнопку «Сказал в реальности»
3. Сохранение метки в AsyncStorage

**Проверка:**
```bash
# Пройти пинг вручную за 60 сек
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement PingScreen with 60-second context flow (Issue #8)"
```

---

### Шаг 9: Issue 9 — RinseScreen (вечер)

**Цель:** Вечерняя рефлексия.

**Действия:**
1. Обновить `RinseScreen.tsx`:
   - Поля: фраза дня, улучшение, успех
   - Сохранение в AsyncStorage
2. Логика автоподбора фразы дня из недавних шагов
3. На следующий день: подтягивание в Warmup

**Проверка:**
```bash
# Заполнить форму и проверить сохранение
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement RinseScreen with daily reflection (Issue #9)"
```

---

### Шаг 10: Issue 10 — Модель "retention-wave"

**Цель:** Волновая память для фраз.

**Действия:**
1. Создать `app/src/lib/retention.ts`:
   - Тип `MemoryLink`
   - Функция `updateWave(link, outcome)`
   - Функция `scheduleNextPing(link)`
2. Интеграция с AsyncStorage для хранения

**Проверка:**
```typescript
// Тесты в любом файле
const link = { phrase: 'test', lastSeen: Date.now(), wave: 1, decayAlpha: 0.8 };
updateWave(link, 'fail');
console.log(link.wave); // Должна уменьшиться
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement retention-wave memory model (Issue #10)"
```

---

### Шаг 11: Issue 11 — Уведомления

**Цель:** Локальные уведомления по расписанию.

**Действия:**
1. Настроить `expo-notifications`:
   - Запрос разрешений
   - Создание уведомлений
2. Deep-linking в `app.json`:
   ```json
   "scheme": "liminal"
   ```
3. Обработка навигации при тапе на уведомление
4. Настройки времени в SettingsScreen

**Проверка:**
```bash
# Создать тестовое уведомление и проверить deep-link
```

**Коммит:**
```bash
git add .
git commit -m "feat: add local notifications with deep-linking (Issue #11)"
```

---

### Шаг 12: Issue 12 — AsyncStorage: события и прогресс

**Цель:** Хранение событий и статистики.

**Действия:**
1. Создать `app/src/lib/storage.ts`:
   - Типы: `UserEvent`, `DailySummary`
   - Функции: `addEvent`, `getStats`, `getStreak`
2. Экран статистики (можно в SettingsScreen)
3. График за 7 дней (простая линия)

**Проверка:**
```typescript
// Добавить несколько событий и проверить статистику
```

**Коммит:**
```bash
git add .
git commit -m "feat: implement event storage and progress tracking (Issue #12)"
```

---

### Шаг 13: Issue 13 — Мини-телеметрия

**Цель:** Локальные метрики.

**Действия:**
1. Логирование:
   - Длительность шагов
   - Отказы (skip)
   - Предпочтения
2. Export в JSON через кнопку в Settings

**Проверка:**
```bash
# Нажать "Export logs" и проверить JSON
```

**Коммит:**
```bash
git add .
git commit -m "feat: add local telemetry with export functionality (Issue #13)"
```

---

### Шаг 14: Issue 14 — Добавить 5 YAML-сцен

**Цель:** Контент для различных контекстов.

**Действия:**
1. Создать YAML файлы в `app/src/data/`:
   - `morning-warmup-01.yaml` (уже есть)
   - `cafe-to-go-01.yaml`
   - `elevator-smalltalk-01.yaml`
   - `standup-update-01.yaml`
   - `pick-up-kid-kindergarten-01.yaml`
2. Валидация каждого через zod

**Проверка:**
```bash
# Пройти каждый сценарий вручную
```

**Коммит:**
```bash
git add .
git commit -m "feat: add 5 YAML scenarios for different contexts (Issue #14)"
```

---

### Шаг 15: Issue 15 — Семейный режим

**Цель:** Режим для занятий с детьми.

**Действия:**
1. Тоггл в SettingsScreen
2. Создать детские сценарии
3. Отдельная вкладка «Family» (условная)
4. Упрощённый UI

**Проверка:**
```bash
# Включить семейный режим и пройти детский сценарий
```

**Коммит:**
```bash
git add .
git commit -m "feat: add family mode with kid-friendly scenarios (Issue #15)"
```

---

## Финальные шаги

### Прогон Acceptance-критериев

- [ ] Утренний сценарий полностью проходим за 3-4 минуты
- [ ] Ping-сцена ≤60 сек
- [ ] Вечерний RINSE работает
- [ ] Уведомления работают с deep-link
- [ ] 5 YAML-сцен валидны
- [ ] Волна снижает частоту повторов

### Сборка build

```bash
# iOS
eas build --platform ios --profile preview

# Android
eas build --platform android --profile preview
```

### Документация

1. Обновить README.md (если нужно)
2. Создать CHANGELOG.md
3. Добавить скриншоты в `docs/screenshots/`

---

## Советы для агентов

### Работа с задачами
- Всегда читайте Acceptance-критерии перед началом
- Коммитьте после каждой завершённой Issue
- Используйте префиксы: `feat:`, `fix:`, `docs:`, `refactor:`

### Отладка
- Используйте `console.log` для быстрой проверки
- Expo Go для тестирования на реальном устройстве
- React DevTools для отладки компонентов

### Тестирование
- Мануальное тестирование критично для UX-потоков
- Юнит-тесты для логики (scoring, retention, etc)
- Проверка на iOS и Android

### Приоритеты
- **P0**: Issues 1-9, 14 — это MVP
- **P1**: Issues 10-13 — важно для retention
- **P2**: Issues 15+ — nice to have

---

## Контакты и поддержка

**Документы:**
- [README.md](./README.md) — общее описание
- [ISSUES.md](./ISSUES.md) — детальный бэклог
- [docs/](./docs/) — доп. документация

**Репозиторий:** GitHub (URL будет добавлен)

---

**Удачи в разработке! 🚀**
