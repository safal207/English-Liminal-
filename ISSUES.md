# ISSUES.md — Готовый бэклог для English-Liminal MVP (Rust + Flutter)

Структурированный список задач для создания 30-дневного MVP с Rust-ядром и Flutter UI.

**Стек:** Rust (core logic) + Flutter (UI) + flutter_rust_bridge (FFI)

---

## Milestone A — Каркас проекта

### Issue A1 — Инициализация Flutter приложения

**Описание:**
Создать Flutter приложение с базовой навигацией (bottom tabs) и заглушками экранов.

**Детали реализации:**
- [ ] Инициализировать Flutter проект (если ещё не создан)
  ```bash
  flutter create app
  ```
- [ ] Настроить `pubspec.yaml` с зависимостями:
  - `provider` (state management)
  - `flutter_tts`, `speech_to_text` (голос)
  - `flutter_local_notifications` (уведомления)
  - `shared_preferences`, `path_provider` (хранилище)
- [ ] Создать экраны (заглушки):
  - `lib/screens/home.dart`
  - `lib/screens/warmup.dart`
  - `lib/screens/ping.dart`
  - `lib/screens/rinse.dart`
  - `lib/screens/settings.dart`
- [ ] Настроить `BottomNavigationBar` в `main.dart`

**Acceptance:**
- [ ] `flutter run` запускает приложение
- [ ] Навигация между 5 экранами работает
- [ ] Все экраны показывают заглушки

**Тесты:**
- [ ] Мануальный: переключение между табами
- [ ] Проверка на iOS и Android

---

### Issue A2 — Настройка flutter_rust_bridge

**Описание:**
Настроить FFI-связь между Rust core и Flutter app.

**Детали реализации:**
- [x] Установить `flutter_rust_bridge_codegen`:
  ```bash
  cargo install flutter_rust_bridge_codegen
  ```
- [x] Добавить `flutter_rust_bridge = "2"` в `core/Cargo.toml`
- [x] Создать `core/src/api.rs` с FFI функциями
  - [x] Script management (load_scripts_from_dir, get_script_ids, get_script_json)
  - [x] Runner functions (start_runner, runner_next, runner_prev, runner_progress, runner_current_step)
  - [x] Storage functions (init_storage, add_event, get_events, export_data)
  - [x] Statistics (get_streak, get_use_in_wild_count)
  - [x] Role management (load_roles_from_dir, get_role_ids, get_role_json, calculate_role_coherence)
  - [x] Health check
- [ ] Сгенерировать Dart биндинги:
  **⚠️ NOTE:** Требуется установить Flutter SDK для генерации биндингов
  ```bash
  flutter_rust_bridge_codegen generate \
    --rust-input crate::api \
    --dart-output app/lib/bridge \
    --rust-root core
  ```
- [ ] Собрать Rust библиотеку для Android:
  ```bash
  cd core
  cargo ndk -t arm64-v8a -o ../app/android/app/src/main/jniLibs build --release
  ```
- [ ] Для iOS: настроить Xcode build script для статической либы

**Acceptance:**
- [ ] Биндинги генерируются без ошибок
- [ ] Вызов `native.healthCheck()` из Dart возвращает "OK"
- [ ] Сборка проходит для iOS и Android

**Тесты:**
- [ ] Юнит-тест: вызов Rust функции из Dart
- [ ] Мануальный: запуск на реальном устройстве

---

## Milestone B — Ядро Rust

### Issue B1 — Модель сценария + парсер YAML

**Описание:**
Реализовать загрузку и валидацию YAML-сценариев в Rust.

**Детали реализации:**
- [x] Создать `core/src/scripts.rs` с типами:
  - `StepType` (enum: Listen, SpeakCheck, Contrast, ApplyToLife)
  - `Step`, `Rehearsal`, `Script`
- [x] Добавить `serde`, `serde_yaml` в `Cargo.toml`
- [ ] Реализовать функцию `load_script(path: &str) -> Result<Script>`
- [ ] Валидация: все поля обязательные, `steps.len() > 0`
- [ ] Интеграция с `api.rs`: `load_scripts_from_dir(dir: String)`

**Acceptance:**
- [ ] Загрузка `assets/scripts/morning-warmup-01.yaml` → Script object
- [ ] Невалидный YAML возвращает понятную ошибку
- [ ] Функция `get_script_ids()` возвращает список ID

**Тесты:**
- [ ] Юнит-тест: парсинг валидного YAML
- [ ] Юнит-тест: ошибка при невалидной схеме
- [ ] Мануальный: вызов из Flutter

---

### Issue B2 — Раннер шагов

**Описание:**
Реализовать state machine для прохождения шагов сценария.

**Детали реализации:**
- [x] Создать `core/src/runner.rs` с типом `RunnerState`:
  - `script_id`, `current_index`, `completed`
- [x] Методы:
  - `new(script)`, `next()`, `prev()`, `progress()`, `reset()`
- [ ] Интеграция с `api.rs`:
  - `start_runner(script_id)` → JSON state
  - `runner_next(state_json)` → новый state
  - `runner_prev(state_json)` → новый state
  - `runner_progress(state_json)` → f32
  - `runner_current_step(state_json)` → JSON step

**Acceptance:**
- [ ] Последовательность шагов корректна
- [ ] Прогресс вычисляется правильно (0.0-1.0)
- [ ] Переходы next/prev работают

**Тесты:**
- [x] Юнит-тест: инициализация + переходы
- [ ] Интеграционный: полный сценарий из Flutter

---

### Issue B3 — Retention-wave

**Описание:**
Реализовать модель волновой памяти с затуханием и планировщиком.

**Детали реализации:**
- [x] Создать `core/src/retention.rs` с типом `MemoryLink`:
  - `phrase`, `last_seen`, `wave`, `decay_alpha`
  - `success_count`, `fail_count`, `use_in_wild_count`
- [x] Функции:
  - `tick(now)` — применить затухание
  - `reinforce(now, gain)` — усилить при успехе
  - `weaken(now)` — ослабить при провале
  - `mark_used_in_wild(now)` — отметить реальное использование
- [x] `next_ping_seconds(link, min, max)` — когда показать следующий пинг
- [x] `calculate_priority(link, now)` — приоритет показа (0-1)

**Acceptance:**
- [x] Затухание работает корректно (wave уменьшается со временем)
- [x] Усиление увеличивает wave
- [ ] Планировщик: слабые фразы → короткий интервал

**Тесты:**
- [x] Юнит-тест: tick, reinforce, weaken
- [x] Юнит-тест: next_ping_seconds возвращает разумные значения
- [ ] Симуляция: 10 фраз, 30 дней

---

### Issue B4 — Хранилище (rusqlite)

**Описание:**
Реализовать локальное хранилище для событий, фраз и сессий.

**Детали реализации:**
- [x] Создать `core/src/storage.rs` с типом `Store`
- [x] Таблицы SQLite:
  - `events(id, ts, kind, payload)`
  - `memory_links(id, phrase, last_seen, wave, decay_alpha, success_count, fail_count, use_in_wild_count)`
  - `sessions(id, script_id, started_at, completed_at, progress)`
- [x] Методы:
  - `add_event(kind, payload)`
  - `save_memory_link(link)`, `load_memory_link(phrase)`
  - `start_session(script_id)`, `complete_session(session_id, progress)`
  - `get_streak()`, `get_use_in_wild_count()`
  - `export_json()` — экспорт всех данных
- [ ] Интеграция с `api.rs`:
  - `init_storage(db_path)`
  - `add_event(kind, payload)`
  - `export_data()` → JSON

**Acceptance:**
- [x] Таблицы создаются при первом запуске
- [x] События сохраняются и читаются корректно
- [ ] Memory links персистентны между запусками
- [x] Экспорт JSON работает

**Тесты:**
- [x] Юнит-тест: add_event, save/load memory_link
- [ ] Интеграционный: инициализация из Flutter
- [ ] Проверка: файл БД создаётся в правильной директории

---

## Milestone C — Флоу MVP

### Issue C1 — Warmup (3 минуты)

**Описание:**
Полный UX-поток утреннего разогрева в Flutter с интеграцией Rust core.

**Детали реализации:**
- [ ] Обновить `lib/screens/warmup.dart`:
  - Загрузка сценария через `native.startRunner()`
  - Отображение текущего шага через `native.runnerCurrentStep()`
  - Прогресс-бар через `native.runnerProgress()`
- [ ] Компоненты для типов шагов:
  - `ListenStep`: TTS воспроизведение
  - `SpeakCheckStep`: запись голоса + оценка
  - `ContrastStep`: выбор варианта
  - `ApplyToLifeStep`: запись своей фразы
- [ ] Навигация: Next/Previous кнопки
- [ ] Таймер 03:00 (опционально)
- [ ] Завершение: сохранение события + показ summary

**Acceptance:**
- [ ] Полный сценарий `morning-warmup-01` проходится за ≤3:30
- [ ] Все типы шагов корректно отображаются
- [ ] Прогресс обновляется в реальном времени
- [ ] События сохраняются в БД

**Тесты:**
- [ ] Мануальный: полный проход сценария
- [ ] Проверка сохранения данных в SQLite

---

### Issue C2 — Ping (60 секунд контекст-пинг)

**Описание:**
Быстрый микро-сценарий: одна фраза + правка + "сказал в реальности".

**Детали реализации:**
- [ ] Обновить `lib/screens/ping.dart`:
  - Показать карточку с одной фразой (из памяти или нового сценария)
  - Цикл: слушай → повтори → оценка (упрощённая)
  - Кнопка «Сказал в реальности» (Use-in-Wild)
- [ ] Интеграция с retention:
  - Выбор фразы через `calculate_priority()`
  - Сохранение `mark_used_in_wild()` при тапе
- [ ] Таймер 60 сек (опционально)

**Acceptance:**
- [ ] Прохождение пинга за ≤60 сек
- [ ] Отметка «сказал в реальности» сохраняется
- [ ] Приоритет фразы обновляется после пинга

**Тесты:**
- [ ] Мануальный: полный цикл пинга
- [ ] Проверка обновления wave в БД

---

### Issue C3 — RINSE (вечер: 1 фраза дня, 1 улучшение, 1 успех)

**Описание:**
Вечерний экран рефлексии.

**Детали реализации:**
- [ ] Обновить `lib/screens/rinse.dart`:
  - Поле «Фраза дня» (автоподбор из недавних шагов)
  - Поле «Одно улучшение» (текст/голос)
  - Поле «Что зашло» (текст/голос)
- [ ] Сохранение в БД через `add_event('rinse', payload)`
- [ ] На следующий день: фраза дня подтягивается в Warmup (будущее)

**Acceptance:**
- [ ] Все поля корректно сохраняются
- [ ] Данные доступны для аналитики
- [ ] Интерфейс быстрый и интуитивный

**Тесты:**
- [ ] Мануальный: заполнение и сохранение
- [ ] Проверка событий в БД

---

### Issue C4 — Локальные уведомления

**Описание:**
Настроить уведомления (утро/день/вечер) с deep-linking.

**Детали реализации:**
- [ ] Интеграция `flutter_local_notifications`
- [ ] Запрос разрешений у пользователя
- [ ] Создание уведомлений:
  - Утро (08:00): Warmup reminder
  - День (12:30): Context ping
  - Вечер (20:00): Rinse reminder
- [ ] Deep-linking: уведомление → конкретный экран
- [ ] Настройки времени в SettingsScreen

**Acceptance:**
- [ ] Уведомления приходят в заданное время
- [ ] Тап по уведомлению открывает нужный экран
- [ ] Время настраивается пользователем

**Тесты:**
- [ ] Мануальный: получение уведомления
- [ ] Мануальный: deep-link работает

---

## Milestone D — Контент и метрики

### Issue D1 — 5 YAML-сцен

**Описание:**
Создать/проверить 5 готовых сценариев для различных контекстов.

**Детали реализации:**
- [x] `morning-warmup-01.yaml` (A/An & H-dropping)
- [x] `cafe-to-go-01.yaml` (Ordering coffee)
- [x] `elevator-smalltalk-01.yaml` (Small talk)
- [x] `standup-update-01.yaml` (Work status update)
- [x] `pick-up-kid-kindergarten-01.yaml` (Picking up kid)
- [ ] Валидация: все YAML корректно парсятся через Rust

**Acceptance:**
- [x] Все 5 сценариев существуют в `assets/scripts/`
- [ ] Каждый сценарий проходит через step runner
- [ ] Контент релевантен и реалистичен

**Тесты:**
- [ ] Мануальный: загрузка каждого сценария
- [ ] Валидация через Rust

---

### Issue D2 — Экран Stats

**Описание:**
Показать пользователю прогресс: streak, use-in-wild, time-to-recall.

**Детали реализации:**
- [ ] Обновить `lib/screens/home.dart` или создать отдельный `stats.dart`
- [ ] Вызовы к Rust:
  - `get_streak()` → текущая серия дней
  - `get_use_in_wild_count()` → сколько раз использовал
  - `get_events()` → последние события
- [ ] UI компоненты:
  - Карточка «Streak» с иконкой огня 🔥
  - Карточка «Used in Wild» с иконкой ✓
  - График за 7 дней (простая линия, можно через fl_chart)

**Acceptance:**
- [ ] Данные отображаются корректно
- [ ] Streak обновляется ежедневно
- [ ] График показывает активность

**Тесты:**
- [ ] Мануальный: проверка отображения
- [ ] Проверка расчёта streak

---

### Issue D3 — Экспорт локального журнала (JSON)

**Описание:**
Кнопка в Settings для экспорта всех данных в JSON.

**Детали реализации:**
- [ ] Обновить `lib/screens/settings.dart`:
  - Кнопка «Export Data»
  - Вызов `native.exportData()` → JSON string
  - Сохранение файла через `path_provider` + `share_plus`
- [ ] Формат JSON:
  ```json
  {
    "events": [...],
    "memory_links": [...],
    "exported_at": "2025-11-11T10:30:00Z"
  }
  ```

**Acceptance:**
- [ ] Кнопка экспорта работает
- [ ] JSON корректно форматирован
- [ ] Файл можно открыть/поделиться

**Тесты:**
- [ ] Мануальный: экспорт и проверка JSON
- [ ] Проверка на iOS и Android

---

## Дополнительные задачи (опционально)

### Issue E1 — Onboarding Flow
- Приветственный экран с объяснением концепции
- Выбор целей (работа, путешествия, семья)
- Настройка расписания уведомлений

### Issue E2 — Dark Mode
- Поддержка тёмной темы в Flutter
- Переключение в Settings

### Issue E3 — Pronunciation Scoring (простой)
- MVP: Levenshtein distance между распознанным и эталонным текстом
- Будущее: фонетический анализ через Rust

### Issue E4 — Shareable Results
- Скриншот streak для шеринга
- Export прогресса в PNG/PDF

---

## Acceptance-критерии для релиза MVP (суммарно)

- [ ] Утренний сценарий полностью проходим за 3-4 минуты
- [ ] Ping-сцена ≤60 сек, с отметкой «сказал в реальности»
- [ ] Вечерний RINSE сохраняет фразу дня и рефлексию
- [ ] Локальные уведомления приходят и ведут в правильный экран
- [ ] 5 YAML-сцен загружаются и работают
- [ ] Локальная retention-wave снижает частоту показа уже закреплённых фраз
- [ ] Rust core работает стабильно на iOS и Android
- [ ] Flutter UI отзывчив и интуитивен

---

## Приоритизация

**P0 (Must Have для MVP):**
- Issue A1-A2 (Flutter + FFI setup)
- Issue B1-B4 (Rust core)
- Issue C1 (Warmup flow)
- Issue D1 (5 YAML scenes)

**P1 (Should Have):**
- Issue C2-C3 (Ping, Rinse)
- Issue C4 (Notifications)
- Issue D2 (Stats)

**P2 (Nice to Have):**
- Issue D3 (Export)
- Issue E1-E4 (Дополнительные фичи)

---

## Phase 2: Role Intelligence (недели 4-8)

### Milestone F — Role Framework

#### Issue F1 — Модель Role в Rust ✅

**Описание:**
Создать `core/src/roles.rs` с типами Role, RoleCoherenceScore, RolePath.

**Детали реализации:**
- [x] Тип `Role` (id, title, goal, benchmarks, scenario_ids)
- [x] Тип `RoleCoherenceScore` с методами:
  - `calculate()` — base + bonuses - penalties
  - `level()` — Novice/Emerging/Developing/Proficient/Mastered
  - `complete_scenario()`, `mark_wild()`, `skip_step()`
- [x] Тип `RolePath` (последовательность ролей)
- [x] Юнит-тесты для coherence score

**Acceptance:**
- [x] Все типы скомпилированы
- [x] Тесты проходят
- [x] API документирован

---

#### Issue F2 — YAML для ролей

**Описание:**
Создать структуру YAML для описания ролей и первый пример.

**Детали реализации:**
- [x] Директория `assets/roles/`
- [x] Схема YAML для Role (id, title, goal, benchmarks, scenario_ids, stages)
- [x] Первая роль: `qa-engineer-abroad.yaml`
- [ ] Валидация через serde_yaml в Rust

**Acceptance:**
- [x] YAML корректно парсится
- [ ] Rust функция `load_role(id)` работает
- [x] Связь роли со сценариями проверена

---

#### Issue F3 — Сценарии для QA роли

**Описание:**
Создать сценарии для роли "QA Engineer Abroad".

**Детали реализации:**
- [x] `qa-interview-technical-01.yaml` — объяснение философии тестирования
- [x] `bug-report-writing-01.yaml` — написание чёткого баг-репорта
- [x] `code-review-feedback-01.yaml` — конструктивный фидбек
- [ ] `release-planning-discussion-01.yaml` — обсуждение рисков релиза
- [x] `standup-update-01.yaml` — уже существует

**Acceptance:**
- [x] 4/5 сценариев созданы
- [ ] Все валидны и проходят через runner
- [x] Контент реалистичен для QA роли

---

#### Issue F4 — Интеграция Role с БД

**Описание:**
Сохранение и загрузка ролей и user progress из SQLite.

**Детали реализации:**
- [ ] Таблица `roles` (id, title, goal, scenario_ids)
- [ ] Таблица `user_roles`:
  ```sql
  CREATE TABLE user_roles(
    user_id TEXT,
    role_id TEXT,
    completed_scenarios INTEGER,
    use_in_wild_count INTEGER,
    skipped_steps INTEGER,
    coherence_score REAL,
    started_at TEXT,
    last_updated TEXT
  );
  ```
- [ ] Методы в `storage.rs`:
  - `save_role_progress(user_id, role_id, score)`
  - `load_role_progress(user_id, role_id) -> RoleCoherenceScore`
  - `get_user_roles(user_id) -> Vec<Role>`

**Acceptance:**
- [ ] Прогресс по ролям персистентен
- [ ] Coherence score обновляется после каждого сценария
- [ ] Данные доступны через FFI

---

#### Issue F5 — FFI API для ролей

**Описание:**
Экспортировать role функции для Flutter через flutter_rust_bridge.

**Детали реализации:**
- [x] В `api.rs` добавить:
  ```rust
  #[frb(sync)]
  pub fn load_roles_from_dir(dir: String) -> Result<u32, String>;

  #[frb(sync)]
  pub fn get_role_ids() -> Vec<String>;

  #[frb(sync)]
  pub fn get_role_json(role_id: String) -> Result<String, String>;

  #[frb(sync)]
  pub fn calculate_role_coherence(
    role_id: String,
    completed_scenarios: u32,
    use_in_wild_count: u32,
    skipped_steps: u32,
  ) -> Result<String, String>; // Returns RoleCoherenceScore JSON
  ```

**Acceptance:**
- [x] Функции реализованы в `core/src/api.rs`
- [ ] Вызов из Flutter возвращает корректные данные (requires Dart bindings generation)
- [ ] Генерация биндингов без ошибок (requires Flutter SDK installation)

---

### Milestone G — Role UI в Flutter

#### Issue G1 — Role Picker Screen

**Описание:**
Экран выбора роли при первом запуске или в Settings.

**Детали реализации:**
- [ ] Создать `lib/screens/role_picker.dart`
- [ ] Загрузка ролей через `native.getRoleIds()`
- [ ] Карточки ролей с:
  - Иконка, заголовок, описание
  - Goal (что достигнешь)
  - Количество сценариев
- [ ] Выбор роли → сохранение в SharedPreferences
- [ ] Интеграция в onboarding flow

**Acceptance:**
- [ ] Все роли отображаются
- [ ] Выбор роли сохраняется
- [ ] После выбора → переход на Home с этой ролью

---

#### Issue G2 — Role Dashboard

**Описание:**
Показать прогресс по текущей роли на Home screen.

**Детали реализации:**
- [ ] Обновить `lib/screens/home.dart`:
  - Карточка текущей роли
  - Coherence Score (круговой прогресс)
  - Список сценариев роли (с чекмарками)
  - Кнопка "Change Role"
- [ ] Загрузка данных через:
  ```dart
  final coherence = await native.calculateRoleCoherence(...);
  final roleJson = await native.getRoleJson(roleId);
  ```

**Acceptance:**
- [ ] Coherence Score отображается визуально
- [ ] Сценарии роли показаны с прогрессом
- [ ] UI интуитивен и красив

---

#### Issue G3 — Coherence Score визуализация

**Описание:**
Круговая/спиральная визуализация уровня владения ролью.

**Детали реализации:**
- [ ] Виджет `CoherenceCircle` (Flutter CustomPainter)
- [ ] Цвета по уровню:
  - Novice: серый
  - Emerging: жёлтый
  - Developing: оранжевый
  - Proficient: зелёный
  - Mastered: золотой
- [ ] Анимация при обновлении score
- [ ] Текст: "75% Proficient"

**Acceptance:**
- [ ] Визуализация красива и понятна
- [ ] Анимация плавная
- [ ] Работает на iOS и Android

---

#### Issue G4 — Liminal Transition Animation

**Описание:**
Анимация перехода к новой роли (когда текущая завершена).

**Детали реализации:**
- [ ] Триггер: когда Coherence Score ≥ 75%
- [ ] Показать модальное окно:
  - "You're ready for the next role!"
  - Визуализация: текущая роль → transition space → новая роль
  - Кнопка "Begin New Role"
- [ ] Анимация: fade + blur + rotate (liminal space aesthetic)
- [ ] Сохранение: старая роль → completed, новая → active

**Acceptance:**
- [ ] Анимация выглядит красиво и "лиминально"
- [ ] Переход к новой роли сохраняется
- [ ] Эмоциональный эффект: момент трансформации

---

### Milestone H — Контент для ролей

#### Issue H1 — QA Engineer (5 сцен) ✅

**Статус:** 4/5 готово

- [x] `qa-interview-technical-01.yaml`
- [x] `standup-update-01.yaml`
- [x] `bug-report-writing-01.yaml`
- [x] `code-review-feedback-01.yaml`
- [ ] `release-planning-discussion-01.yaml`

---

#### Issue H2 — Visa Applicant (3 сцены)

**Описание:**
Роль для прохождения визового интервью.

**Сценарии:**
- [ ] `visa-interview-purpose-01.yaml` — объяснение цели поездки
- [ ] `visa-interview-ties-01.yaml` — доказательство связей с родиной
- [ ] `visa-interview-financials-01.yaml` — финансовая состоятельность

**Role YAML:**
```yaml
id: visa-applicant
title: "Visa Applicant"
goal: "Pass visa interview confidently"
```

---

#### Issue H3 — Diplomatic Communicator (2 сцены)

**Описание:**
Деликатное общение в конфликтных ситуациях.

**Сценарии:**
- [ ] `diplomatic-disagreement-01.yaml` — несогласие без конфронтации
- [ ] `diplomatic-apology-01.yaml` — искреннее извинение

---

#### Issue H4 — Hospitality Professional (3 сцены)

**Описание:**
Работа в индустрии гостеприимства.

**Сценарии:**
- [ ] `hospitality-greeting-guest-01.yaml` — приветствие гостя
- [ ] `hospitality-complaint-handling-01.yaml` — работа с жалобами
- [ ] `hospitality-upselling-01.yaml` — предложение дополнительных услуг

---

#### Issue H5 — Family Abroad (2 сцены)

**Описание:**
Жизнь с семьёй в англоязычной стране.

**Сценарии:**
- [ ] `family-parent-teacher-meeting-01.yaml` — встреча с учителем
- [ ] `family-doctor-appointment-01.yaml` — визит к врачу с ребёнком

---

### Milestone I — Role Paths

#### Issue I1 — RolePath в Rust ✅

**Статус:** Уже реализовано в `core/src/roles.rs`

- [x] Тип `RolePath` (id, title, role_ids)
- [x] Методы: `next_role()`, `progress()`
- [x] Тесты

---

#### Issue I2 — YAML для путей

**Описание:**
Создать файлы YAML для ролевых путей.

**Примеры путей:**
```yaml
# assets/paths/work-abroad.yaml
id: work-abroad
title: "Work Abroad"
role_ids:
  - visa-applicant
  - job-seeker
  - qa-engineer
  - senior-qa
  - technical-mentor
```

**Детали:**
- [ ] `work-abroad.yaml`
- [ ] `study-abroad.yaml`
- [ ] `family-life-abroad.yaml`

---

#### Issue I3 — Path Selection UI

**Описание:**
Экран выбора пути в onboarding.

**Детали:**
- [ ] Создать `lib/screens/path_selection.dart`
- [ ] 3 карточки путей
- [ ] Визуализация: линия/спираль ролей в пути
- [ ] Выбор → сохранение → открытие первой роли

---

#### Issue I4 — Path Progress Tracking

**Описание:**
Отслеживание прогресса по пути в БД и UI.

**Детали:**
- [ ] Таблица `user_paths` в SQLite
- [ ] Виджет `PathProgressBar` — сколько ролей завершено
- [ ] На Home screen показать: "3/5 roles completed in 'Work Abroad'"

---

## Приоритизация (обновлённая)

**P0 (Must Have для MVP):**
- Issue A1-A2 (Flutter + FFI setup)
- Issue B1-B4 (Rust core)
- Issue C1 (Warmup flow)
- Issue D1 (5 YAML scenes)

**P1 (Should Have):**
- Issue C2-C3 (Ping, Rinse)
- Issue C4 (Notifications)
- Issue D2 (Stats)
- **Issue F1-F3 (Role Framework foundation)** ✅

**P2 (Phase 2 - Role Intelligence):**
- Issue F4-F5 (Role storage + FFI)
- Issue G1-G4 (Role UI)
- Issue H1-H5 (Content for roles)
- Issue I1-I4 (Role Paths)

**P3 (Phase 3 - Social Impact):**
- Community scenes
- Social metrics
- Mission integration

---

**Следующий шаг:**
1. **Immediate:** Issue A2 → FFI биндинги
2. **After MVP:** Issue F4 → Role storage integration
